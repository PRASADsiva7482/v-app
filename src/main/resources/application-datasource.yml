datasource:
   crmdatasource:
   -  name: initial
      jdbcUrl: ${DB_CRM_URL:jdbc:mysql://localhost:3306/v_app?zeroDateTimeBehavior=convertToNull&autoReconnect=true&useSSL=true&nullNamePatternMatchesAll=true&useUnicode=yes&requireSSL=true&allowPublicKeyRetrieval=true&sessionVariables=sql_require_primary_key=0&characterEncoding=UTF-8}
      driverClassName: ${DB_DRIVER:com.mysql.cj.jdbc.Driver}
      username: ${DB_CRM_USERNAME:root}
      password: ${DB_CRM_PASSWORD}
      poolName: CRM-POOL
      maximumPoolSize: ${DB_POOL_MAX_SIZE:10}
      minimumIdle: ${DB_POOL_MIN_IDLE:10}
   -  name: crmcellcard
      jdbcUrl: ${DB_CRM_CELLCARD_URL:jdbc:mysql://localhost:3306/v_app?zeroDateTimeBehavior=convertToNull&autoReconnect=true&useSSL=true&nullNamePatternMatchesAll=true&useUnicode=yes&requireSSL=true&allowPublicKeyRetrieval=true&sessionVariables=sql_require_primary_key=0&characterEncoding=UTF-8}
      driverClassName: ${DB_DRIVER:com.mysql.cj.jdbc.Driver}
      username: ${DB_CRM_CELLCARD_USERNAME:root}
      password: ${DB_CRM_CELLCARD_PASSWORD}
      poolName: CRM-POOL-CELLCARD
      maximumPoolSize: ${DB_POOL_MAX_SIZE:10}
      minimumIdle: ${DB_POOL_MIN_IDLE:10}
   keycloakdatasource:
   -  name: initial
      jdbcUrl: ${DB_KEYCLOAK_URL:jdbc:mysql://localhost:3306/keycloak?zeroDateTimeBehavior=convertToNull&autoReconnect=true&useSSL=true&nullNamePatternMatchesAll=true&useUnicode=yes&requireSSL=true&allowPublicKeyRetrieval=true&sessionVariables=sql_require_primary_key=0&characterEncoding=UTF-8}
      driverClassName: ${DB_DRIVER:com.mysql.cj.jdbc.Driver}
      username: ${DB_KEYCLOAK_USERNAME:root}
      password: ${DB_KEYCLOAK_PASSWORD}
      poolName: KEYCLOAK-POOL
      maximumPoolSize: ${DB_POOL_MAX_SIZE:10}
      minimumIdle: ${DB_POOL_MIN_IDLE:10}
   -  name: keycloakcellcard
      jdbcUrl: ${DB_KEYCLOAK_CELLCARD_URL:jdbc:mysql://localhost:3306/keycloak?zeroDateTimeBehavior=convertToNull&autoReconnect=true&useSSL=true&nullNamePatternMatchesAll=true&useUnicode=yes&requireSSL=true&allowPublicKeyRetrieval=true&sessionVariables=sql_require_primary_key=0&characterEncoding=UTF-8}
      driverClassName: ${DB_DRIVER:com.mysql.cj.jdbc.Driver}
      username: ${DB_KEYCLOAK_CELLCARD_USERNAME:root}
      password: ${DB_KEYCLOAK_CELLCARD_PASSWORD}
      poolName: KEYCLOAK-POOL-CELLCARD
      maximumPoolSize: ${DB_POOL_MAX_SIZE:10}
      minimumIdle: ${DB_POOL_MIN_IDLE:10}
